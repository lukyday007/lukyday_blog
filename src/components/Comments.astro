---
import type { Props } from '@astrojs/starlight/props';
import Default from '@astrojs/starlight/components/LastUpdated.astro';

const { locale } = Astro.props;
const giscusLang = locale === 'en' ? 'en' : 'ko';
---

<Default {...Astro.props} />

<div class="giscus-container">
  <script src="https://giscus.app/client.js"
          data-repo="lukyday007/lukyday_blog"
          data-repo-id="R_kgDORmJz6A"
          data-category="Announcements"
          data-category-id="DIC_kwDORmJz6M4C4Uat"
          data-mapping="pathname"
          data-strict="0"
          data-reactions-enabled="1"
          data-emit-metadata="0"
          data-input-position="bottom"
          data-theme="preferred_color_scheme"
          data-lang={giscusLang} 
          crossorigin="anonymous"
          async>
  </script>
</div>

<style>
  .giscus-container {
    margin-top: 3rem;
    width: 100%;
  }
  .giscus-container :global(iframe) {
    width: 100% !important;
  }
</style>

<script is:inline>
  document.addEventListener('astro:page-load', () => {
    const iframe = document.querySelector('iframe.giscus-frame');
    if (!iframe) return;
    
    // 현재 URL에서 언어 감지
    const lang = location.pathname.startsWith('/en/') ? 'en' : 'ko';
    
    iframe.contentWindow.postMessage({
      giscus: { setConfig: { lang: lang } }
    }, 'https://giscus.app');
  });
</script>